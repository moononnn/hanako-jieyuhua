# CHANGELOG

## v1.0.1 (2026-08-16)

🌸 **提问弹窗不再“看人下菜”**

- 🔧 **修复悬浮球模式下其他助手不弹提问**：以前只靠工具描述引导助手调用提问工具，模型在长工具列表里容易漏掉，需要拍板时只发文本不弹窗。现在悬浮球模式下注入轻量拍板引导（不干扰推荐区），并强化工具描述——任何助手的会话里，需要拍板就会弹窗

## v1.0.0 (2026-08-15)

🌸 **桌面解语花会提问了**

- ❓ **提问弹窗**：助手需要你拍板时，桌面解语花直接弹出提问面板——选项、自定义答案、跳过随你选，回答自动回传给助手继续干活（交互参考了 dsh 的 ask_user_question；提问依赖悬浮球，需要先启动桌面解语花才生效）
- 🪟 **归属窗口标注**：多窗口时提问面板标明「来自窗口：xxx」，一眼知道谁在等你
- 🗣️ **隐式跳过改静默**：无视提问继续聊天 = 提问静默作废，零弹窗零消息零打扰；判定只看提问归属的窗口，多窗口忙别的不会误杀提问
- ✨ **提问面板视觉强化**：❓ 标题、问题浅色块、弹出时标题脉冲、花瓣持续散发提醒；选项竖排整行横条胶囊（推荐条同款）、俏皮输入框文案
- 📏 **面板高度按内容撑起**：提问面板按真实内容高度展开，选项不再被滚动区压矮
- 🌀 **确认式折叠**：提问挂起时第一次点球只闪烁提醒，第二次才收起，手滑不丢题
- 🗑️ **折叠 = 放弃**：折叠即静默作废（不回传不唤醒），比当前题更旧的堆积题一起清空，重启残留的历史提问一次折叠全干净；折叠过的题彻底不再弹出（菜单开着也拦）

## v0.7.0 (2026-08-14)

🌸 **桌面解语花更有生命感**

- 🌬️ **连续鼠标风场**：鼠标在花朵附近掠过时，花枝、花朵、跟随叶随掠过速度与左右/上下方向摆动，像真有风吹过
- 🍃 **掠风碎瓣**：快速掠过时带起 3/5/7/9 枚小簇碎瓣，数量、散落距离、旋转和回落轨迹随鼠标速度与方向变化，比点击更轻盈
- 💥 **点击大簇**：按住花朵枝条压弯蓄力，松手越界回弹，伴随两次放射花瓣簇（按下 11 枚 + 松手 16 枚）
- 🌸 **悬停碎瓣**：鼠标悬停时花朵会间歇飘落一两枚碎瓣，不再只是安静绽放
- 🎯 **悬停更跟手**：命中区从整块透明窗口收紧到可见花枝轮廓，鼠标真正靠近花朵才触发入场阵风

✍️ **推荐回复更像人**

- 📝 **口语质量准则**：提示词加入「杀八股」规则——不打比方、不堆大词、不用「不是…而是…」这类句式、不空泛敷衍、不对自家助手客套
- 🛡️ **八股过滤**：生成后逐条扫描，命中八股模式的推荐直接剔除（宁缺毋滥），不够条数自动补生成一次；悬浮球与卡片同一套标准
- 🧩 **JSON 截断兜底**：模型输出被截断时，从残缺文本里挖出完整的推荐条目，尽量救回几条

## v0.6.0 (2026-08-14)

- 🏷️ **会话标题**：悬浮球新增「会话标题」板块——「重命名标题」总结整段对话生成新标题写回宿主，改错窗口也不怕，「退回」一键还原上一次的标题；标题语言跟随对话（英文对话自动生成英文标题）
- 👁️ **当前会话可见**：悬浮球头部新增一行显示正在读取的是哪个对话框（自动判断或手动固定都看得见），切换/重命名后实时同步
- ✨ **推荐更贴人**：推荐提示词升级——模仿用户说话的风格（语气词、口头禅、用词习惯），语言跟随对话；卡片与悬浮球共用同一份提示词，不再维护两份
- 🎨 **面板 UI 打磨**：重命名/退回按钮与「刷新推荐」同款实心样式；板块加小标题；底部模式文案带「当前模式：」前缀并随切换实时同步；重命名成功两行反馈（新标题 + 刷新提示）

## v0.5.1 (2026-08-12)

- 🐛 修复部分模型返回逐行 JSON 时，推荐卡片会原样显示 `text`、`direction` 等结构字段
- 🛡️ 加强推荐结果兼容：支持 JSONL、常见包裹数组、带尾逗号多行数组，并过滤模型前缀说明和括号噪音
- ✏️ 收紧生成格式要求，卡片与桌面解语花统一要求输出完整 JSON 数组
- ✏️ 设置页与测试文案统一使用「小花」称呼

## v0.5.0 (2026-08-11)

- 🎯 **推荐目标选择器重构**：对话选择并入推荐面板，点开即选「自动判断 / 固定一段」，候选收为最近活跃的 5 个对话，不再弹第二层窗口
- 💡 选择器可点性指引：右上角新增「这里可以选择按哪段对话推荐回复 →」引导句，一眼知道这里能选
- 🐛 修复推荐上下文被超长系统注入污染：截断上限提高到 8000 字符，未闭合注入块整体丢弃，推荐不再答非所问
- 🐛 修复多窗口时「跟随最近」失效：时间戳字符串比较恒为假，改毫秒数比较
- 🐛 修复对话选择器列表偶发空白：异步线程改经信号回主线程渲染，会话名优先读真实标题
- 🐛 修复推荐目标状态竞态：切换以服务端确认为准，旧响应不覆盖新选择
- ✏️ 刷新提示文案更通顺：「正在取推荐…」→「正在获取推荐回复…」

## v0.4.0 (2026-08-11)

- 🌸 **正式更名「解语花」**（原「接个话」）：设置页、悬浮球、卡片、提示文案全部统一为新名字；插件 id 不变，已装用户更新后无感迁移
- 🏷️ 悬浮球「跟随当前对话」标签显示助手友好名（读各用户自己配置的助手名），不再显示英文 id
- 🐛 修复悬浮球打开时标签卡在「读取中」：缓存秒开路径补上目标会话信息
- 📍 修复悬浮球弹出窗锚点漂移：花朵固定在面板上部 38% 处（面板主体在花下方），每次打开/拖动/重启位置一致
- 🌸 README 新增桌面解语花（樱花悬浮球）功能说明

## v0.2.0 (2026-08-07)

- ✨ 推荐卡片跟随 Hana 夜间主题：使用 midnight / midnight-contrast 主题时，卡片自动切换为深色配色（优先引用宿主主题变量，未传主题样式时退回内置暗色值），不再晃眼

## v0.1.0 (2026-08-06)

- 🎉 首发：小花回复后生成推荐回复卡片，点一下复制或直接发送
