# CHANGELOG

## v1.0.59 (2026-08-28)

🌸 **断联续接 + 悬浮球目标与层级加固**

- 🔁 **断联续接**：对话因超时、模型报错、自动重试耗尽或异常释放而中断时，桌面解语花弹出「窗口断联了」小卡，一键发送「继续」接回话头；自动续接可选开启，并带冷却、连续失败降级和失败回退待办，避免反复打扰
- 🧹 **断联状态收口**：用户主动停止、自己重新发言和自动重试成功都不会留下旧待办；同一窗口只保留最新提醒，过期待办自动清理
- 🛡️ **目标会话更安全**：固定目标只接受真实 sessions 目录内的 JSONL 文件，拒绝目录外路径、符号链接越界和已失效会话；发送时只使用宿主认可的会话身份，解析不到时交给宿主按路径判断
- 🎯 **目标列表更可靠**：合并宿主窗口与本地扫描结果，跳过插件主动推送造成的假活跃，兼容秒级时间戳；切换目标后，旧的异步回包不会再覆盖新选择
- 🪟 **断联卡层级修复**：首次送达仍会及时置前，同一张卡后续轮询不再反复顶到其他软件上方；收起后恢复普通互动层
- 🎨 **滚动条统一**：设置页与悬浮球面板使用细薄荷圆形滚动条，明暗主题保持一致
- ✅ **回归验证**：Node 303 项、Python 7 个测试文件全绿，覆盖断联事件、状态机、待办队列、防风暴、目标安全、异步竞态和断联卡离屏交互

## v1.0.54 (2026-08-25)

🌸 **提问弹窗、多选确认与悬浮球体验修复**

❓ **提问体验**

- 🧭 **单选 / 多选分流**：需要拍板时按真实语义弹出单选或多选题，多选支持最少/最多选择数量，确认后以选项数组回传
- 🧘 **关闭重启不闪回旧题**：用户继续聊天后，旧提问会静默作废；悬浮球关闭再重启也不会把历史题重新弹出来
- 🧹 **并发清理加固**：清理延迟或写入等待时，展示层先过滤已跳过的旧题；清理通道有有限等待，失败会留到下一轮重试，不会永久卡住
- 🧩 **引导按状态注入**：只有原版或融合悬浮球实时运行时才注入拍板引导，普通对话继续自然回答

🖱️ **悬浮球与面板**

- 🌬️ **拖动更有手感**：花梗、花冠和叶片按真实拖动速度产生分层滞后、急停回弹与自然衰减
- 🪟 **面板定位更稳**：推荐面板和朗读窗优先放在花朵左侧，空间不足时再翻到右侧；拖动面板时保留相对位置
- 🎯 **多选焦点更干净**：键盘焦点不再额外套一层厚矩形框，只保留选项自身的选中态
- ✅ **回归测试补齐**：覆盖多选数组、单选即时回传、隐式跳过、关闭重启、并发清理与面板交互

## v1.0.46 (2026-08-23)

🌸 **语音朗读、闲不住融合与全面兼容升级**

🎙️ **语音朗读**

- 🎧 **朗读最新回复**：悬浮球面板一键把目标对话里小花最近的回复读出来（播放中再点停止，面板收起自动停）
- 🪟 **独立朗读窗**：朗读拆成专用弹窗，集中播放/暂停/继续、↻ 重听、♪ 读新、♡ 收藏和完整文本预览；与推荐面板同一套贴球定位（默认左侧、贴边自动翻边、38% 锚点），拖动花朵同步移动（拖过不拽回）
- 🎯 **窗口与回复选择**：朗读窗支持自动判断/手动固定最近 5 个窗口，可在最新回复与前 5 条助手回复间自由切换；列表与合成固定同一会话，手动选旧回复带身份标记，新回复插入不错位
- 🔁 **刷新入口**：回复选择区新增「↻ 刷新」，显式绑定当前会话重新读取最新回复；刷新不自动朗读，并清掉旧音频/收藏/选中状态
- 🗂️ **语音收藏**：主页新增「语音收藏」入口，按助手分组展示、可试听/删除；收藏记录来源助手，旧收藏归入「其他」分组，界面手帐化（圆标分组头、卡片条目、两段确认删除）
- 🎛️ **三档模型配置**：自动用 Hana 已配置的语音模型（MiMo 协议，零配置）/ 手动从候选列表选 / 自定义双协议（MiniMax t2a_v2 + OpenAI 兼容）；音色按协议区分，旧 MiniMax 配置自动迁移，支持按助手配专属音色
- 🔊 **试听走系统播放**：Hana webview 里网页 Audio 被自动播放限制，试听改为后端生成音频走系统播放（wav→.NET SoundPlayer），设置页试听不再依赖网页
- 🐛 修复 MiniMax 电锯声（HEX/Base64 双兼容）、朗读全文过滤隐藏元信息（MOOD/思考块不读出来）、先选后合成（打开弹窗不再自动烧一次合成）

🔗 **闲不住融合**

- 🤝 **融合桥**：悬浮球提供 `jiegehua:fusion:v1` 桥，配合闲不住协调器启停、互斥与面板状态恢复（Ask/朗读入口）；融合球关闭后原版可正常启动，不会误拦或重复拉起
- 🔄 **拆回不打架**：旧代次失效、transition 互斥、强制退出兜底，融合来回切换不残留重复进程

❓ **提问体验**

- 🧭 **意图分流**：只在你明确要拍板时才弹提问面板，了解类话题直接作答，绝不改写成选择题
- ✍️ **先正文后弹窗**：先自然写完回复正文，确实需要拍板再弹窗，不再一弹窗就只递选项显得生硬焦急；弹窗失败时降级为普通对话指引
- 🧘 **作答后安静收起**：选选项/自定义/跳过后短暂反馈直接收成球，不弹回推荐面板；无视提问继续聊天 = 静默作废，零打扰

🃏 **推荐卡片**

- ✨ **新版内联卡**：适配新版 Hana，推荐渲染成真正的内联 Interactive Card（show_card + SessionFile）；较旧公开版自动走兼容 iframe 卡片，老版本不丢功能
- 🛟 **发送失败兜底**：Card Host 暂时不可用时自动退回复制并明确提示，消息不悄悄丢掉；发送结果与 pending 状态正确回滚
- 🌐 **语言规则锁定**：推荐语言跟随对话（中文对话固定中文），不再让模型自己判断导致偶发纯英文；卡片与悬浮球共用同一份提示词
- 📋 **复制模式回退纯手动**：点推荐只复制到剪贴板自己粘（移除窗口模拟点击/粘贴，分享版不碰杀毒软件误报风险）

🔐 **凭据安全**

- 🔑 **API Key 加密存储**：Windows 下自定义 API Key 用系统 DPAPI 加密落盘；页面与接口只返回「已保存」掩码，留空保存不覆盖旧 Key，可明确清除
- 🔄 **旧 Key 自动迁移**：旧版本 `enc:` 混淆存量启动后自动迁移，不会突然要求重新输入

🔍 **会话定位与性能**

- 📏 **长会话兼容**：从会话文件尾按完整行持续向前扫描，修复超过 256KB 的长会话误选旧窗口、推荐/朗读读不到当前对话的问题
- ⚡ **mtime 粗筛**：找最近活跃对话只读最近 60 个候选文件尾部，不再全量扫全部会话文件，降低 IO 压力
- 🏷️ **标题安全**：隐藏思考块不进标题；生成失败时清理附件包装，拒绝把 fileId 元数据当标题；还原按钮只在有新标题时显示

🎨 **界面与体验**

- 🗂️ **主面板重排**：顶部当前对话作用域 + 推荐回复主区 + 对话工具卡片（朗读/标题），统一骨架可继续扩展
- 🌸 **悬浮球缩小至 90%**：可见内容整体收小，透明窗口与交互定位不变
- 🌱 **开设置页自动启动悬浮球**：仅当展示方式选「解语花」时自动启动，手动关过则本次不弹，融合球在跑时静默不打扰

## v1.0.3 (2026-08-18)

🌸 **提问完安静收工，不再打扰你**

- 🔇 **作答后直接收起**：在提问面板里选选项、填自定义答案或点跳过后，面板短暂反馈一下就直接收成悬浮球，不再弹回推荐面板（以前作答完会「恢复推荐弹窗」，显别扭）；下次想看重启点开球即可
- 🧘 **无视提问 = 静默收起**：提问挂起时你直接在主对话框继续说话，面板会自动收成球，不弹推荐、零打扰（修复了历史消息里的「提问卡片」字样导致隐式跳过失效的问题）
- 🔒 **面板收起不丢状态**：收起后再点开，推荐缓存照常恢复，上次的推荐还在

## v1.0.2 (2026-08-17)

🌸 **悬浮球弹窗更懂你的鼠标**

- 🌬️ **鼠标离开自动半透明**：左键面板与右键浮签光标移出后稍候缓慢淡出（下限 60%，留存在感也找得到窗口），移回快速恢复；刚弹出时给短暂缓冲期，光标还没进去也不会立刻缩退
- ❓ **提问面板保持实体**：ask 提问态不参与淡出，答案选项永远清晰可见，退出提问恢复推荐后淡出机制照常生效
- 🖱️ **右键交互统一**：单击右键展开浮签、再次右键收起，与左键面板的 toggle 习惯一致；改由应用级事件接管「点击空白处自动关闭」，原失焦关闭体验不变
- 🧲 **左右键弹窗互斥**：开一个自动收另一个，两窗不再叠放遮挡，hover 恢复也不会失效

## v1.0.1 (2026-08-16)

🌸 **提问弹窗不再“看人下菜”**

- 🔧 **修复悬浮球模式下其他助手不弹提问**：以前只靠工具描述引导助手调用提问工具，模型在长工具列表里容易漏掉，需要拍板时只发文本不弹窗。现在悬浮球模式下注入轻量拍板引导（不干扰推荐区），并强化工具描述——任何助手的会话里，需要拍板就会弹窗

## v1.0.0 (2026-08-15)

🌸 **桌面解语花会提问了**

- ❓ **提问弹窗**：助手需要你拍板时，桌面解语花直接弹出提问面板——选项、自定义答案、跳过随你选，回答自动回传给助手继续干活（交互参考了 dsh 的 ask_user_question；提问依赖悬浮球，需要先启动桌面解语花才生效）
- 🪟 **归属窗口标注**：多窗口时提问面板标明「来自窗口：xxx」，一眼知道谁在等你
- 🗣️ **隐式跳过改静默**：无视提问继续聊天 = 提问静默作废，零弹窗零消息零打扰；判定只看提问归属的窗口，多窗口忙别的不会误杀提问
- ✨ **提问面板视觉强化**：❓ 标题、问题浅色块、弹出时标题脉冲、花瓣持续散发提醒；选项竖排整行横条胶囊（推荐条同款）、俏皮输入框文案
- 📏 **面板高度按内容撑起**：提问面板按真实内容高度展开，选项不再被滚动区压矮
- 🌀 **确认式折叠**：提问挂起时第一次点球只闪烁提醒，第二次才收起，手滑不丢题
- 🗑️ **折叠 = 放弃**：折叠即静默作废（不回传不唤醒），比当前题更旧的堆积题一起清空，重启残留的历史提问一次折叠全干净；折叠过的题彻底不再弹出（菜单开着也拦）

## v0.7.0 (2026-08-14)

🌸 **桌面解语花更有生命感**

- 🌬️ **连续鼠标风场**：鼠标在花朵附近掠过时，花枝、花朵、跟随叶随掠过速度与左右/上下方向摆动，像真有风吹过
- 🍃 **掠风碎瓣**：快速掠过时带起 3/5/7/9 枚小簇碎瓣，数量、散落距离、旋转和回落轨迹随鼠标速度与方向变化，比点击更轻盈
- 💥 **点击大簇**：按住花朵枝条压弯蓄力，松手越界回弹，伴随两次放射花瓣簇（按下 11 枚 + 松手 16 枚）
- 🌸 **悬停碎瓣**：鼠标悬停时花朵会间歇飘落一两枚碎瓣，不再只是安静绽放
- 🎯 **悬停更跟手**：命中区从整块透明窗口收紧到可见花枝轮廓，鼠标真正靠近花朵才触发入场阵风

✍️ **推荐回复更像人**

- 📝 **口语质量准则**：提示词加入「杀八股」规则——不打比方、不堆大词、不用「不是…而是…」这类句式、不空泛敷衍、不对自家助手客套
- 🛡️ **八股过滤**：生成后逐条扫描，命中八股模式的推荐直接剔除（宁缺毋滥），不够条数自动补生成一次；悬浮球与卡片同一套标准
- 🧩 **JSON 截断兜底**：模型输出被截断时，从残缺文本里挖出完整的推荐条目，尽量救回几条

## v0.6.0 (2026-08-14)

- 🏷️ **会话标题**：悬浮球新增「会话标题」板块——「重命名标题」总结整段对话生成新标题写回宿主，改错窗口也不怕，「退回」一键还原上一次的标题；标题语言跟随对话（英文对话自动生成英文标题）
- 👁️ **当前会话可见**：悬浮球头部新增一行显示正在读取的是哪个对话框（自动判断或手动固定都看得见），切换/重命名后实时同步
- ✨ **推荐更贴人**：推荐提示词升级——模仿用户说话的风格（语气词、口头禅、用词习惯），语言跟随对话；卡片与悬浮球共用同一份提示词，不再维护两份
- 🎨 **面板 UI 打磨**：重命名/退回按钮与「刷新推荐」同款实心样式；板块加小标题；底部模式文案带「当前模式：」前缀并随切换实时同步；重命名成功两行反馈（新标题 + 刷新提示）

## v0.5.1 (2026-08-12)

- 🐛 修复部分模型返回逐行 JSON 时，推荐卡片会原样显示 `text`、`direction` 等结构字段
- 🛡️ 加强推荐结果兼容：支持 JSONL、常见包裹数组、带尾逗号多行数组，并过滤模型前缀说明和括号噪音
- ✏️ 收紧生成格式要求，卡片与桌面解语花统一要求输出完整 JSON 数组
- ✏️ 设置页与测试文案统一使用「小花」称呼

## v0.5.0 (2026-08-11)

- 🎯 **推荐目标选择器重构**：对话选择并入推荐面板，点开即选「自动判断 / 固定一段」，候选收为最近活跃的 5 个对话，不再弹第二层窗口
- 💡 选择器可点性指引：右上角新增「这里可以选择按哪段对话推荐回复 →」引导句，一眼知道这里能选
- 🐛 修复推荐上下文被超长系统注入污染：截断上限提高到 8000 字符，未闭合注入块整体丢弃，推荐不再答非所问
- 🐛 修复多窗口时「跟随最近」失效：时间戳字符串比较恒为假，改毫秒数比较
- 🐛 修复对话选择器列表偶发空白：异步线程改经信号回主线程渲染，会话名优先读真实标题
- 🐛 修复推荐目标状态竞态：切换以服务端确认为准，旧响应不覆盖新选择
- ✏️ 刷新提示文案更通顺：「正在取推荐…」→「正在获取推荐回复…」

## v0.4.0 (2026-08-11)

- 🌸 **正式更名「解语花」**（原「接个话」）：设置页、悬浮球、卡片、提示文案全部统一为新名字；插件 id 不变，已装用户更新后无感迁移
- 🏷️ 悬浮球「跟随当前对话」标签显示助手友好名（读各用户自己配置的助手名），不再显示英文 id
- 🐛 修复悬浮球打开时标签卡在「读取中」：缓存秒开路径补上目标会话信息
- 📍 修复悬浮球弹出窗锚点漂移：花朵固定在面板上部 38% 处（面板主体在花下方），每次打开/拖动/重启位置一致
- 🌸 README 新增桌面解语花（樱花悬浮球）功能说明

## v0.2.0 (2026-08-07)

- ✨ 推荐卡片跟随 Hana 夜间主题：使用 midnight / midnight-contrast 主题时，卡片自动切换为深色配色（优先引用宿主主题变量，未传主题样式时退回内置暗色值），不再晃眼

## v0.1.0 (2026-08-06)

- 🎉 首发：小花回复后生成推荐回复卡片，点一下复制或直接发送
