# 解语花测试说明

## Node 回归

```powershell
node --test --test-concurrency=1 tests/*.test.js
```

覆盖：提问状态与回传、单选/多选模式与数量约束、分支桥、配置迁移、推荐解析、模型/语音配置、会话选择、助手名/会话路径补全、目标列表超时回退、朗读清洗、朗读回复刷新/当前 sessionPath 绑定/路径漂移拒绝/选中后再读、朗读收藏快照与旧回包丢弃、收藏请求体大小边界、发送幂等与失败回滚、卡片发送结果封装与复制兜底、悬浮球目标与数据边界、融合状态桥与原版启动互斥、observer ask 引导实时运行状态门、长会话超过 256KB 后仍能定位并读取最新用户消息、跨 64KB UTF-8 边界/无最终换行/截断尾行；Python 悬浮球测试另覆盖标题「还原」按钮的生成后显示/退回后隐藏、朗读窗与花朵双向拖动、朗读目标列表显示助手名、刷新失败重试和收藏按钮幂等状态、单选/多选提问渲染与勾选上限、窗口真实拖速平滑/限幅、触边/反向/急停、极短 flick 余势、花梗/花冠/叶片分层弹簧、纵向滞后、点击转拖动不误触释放爆瓣，以及主面板/朗读窗带球拖动进入同一物理链；融合球测试覆盖融合页同样继承该行为。

最近结果：2026-08-25，Node 263 项、Python 96 项通过；新增 `/ask/pending` 等待隐式跳过清理、并发轮询复用清理 Promise、并发写入时过滤 skip 队列旧题、skip 落盘有限等待、PyQt 多选确认提交数组、单选点击即时回传与关闭重启回归。上一轮的 ask 单选/多选协议、数组 Deferred 回传、实时悬浮球状态门和拖动联动回归继续全绿；全量 `tests/run_all.py` 在 Windows 按文件隔离运行稳定通过。

## Python 悬浮球

```powershell
python -m py_compile python/zhujian_app.py
python tests/run_all.py
```

融合协议相关状态通过 `fusionPanel` 落盘；离屏自动测试覆盖物理与窗口联动，真实鼠标手感、Ask/朗读继承仍需重启 Hana 后实机验证。
