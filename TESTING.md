# 解语花测试说明

## Node 回归

```powershell
node --test --test-concurrency=1 tests/*.test.js
```

覆盖：提问状态与回传、分支桥、配置迁移、推荐解析、模型/语音配置、会话选择、助手名/会话路径补全、目标列表超时回退、朗读清洗、朗读回复刷新/当前 sessionPath 绑定/路径漂移拒绝/选中后再读、朗读收藏快照与旧回包丢弃、收藏请求体大小边界、发送幂等与失败回滚、卡片发送结果封装与复制兜底、悬浮球目标与数据边界、融合状态桥与原版启动互斥、长会话超过 256KB 后仍能定位并读取最新用户消息、跨 64KB UTF-8 边界/无最终换行/截断尾行；Python 悬浮球测试另覆盖标题「还原」按钮的生成后显示/退回后隐藏、朗读窗与花朵双向拖动、朗读目标列表显示助手名、刷新失败重试和收藏按钮幂等状态，融合球测试覆盖融合页同样继承该行为。

最近结果：2026-08-23，256 项通过；新增朗读回复刷新、移除旧的“朗读最新回复”动作、融合入口复用和朗读窗相对位置审查回归。收藏数据新增来源助手标签（agentId）与按助手分组（groupFavorites），覆盖存储/分组/旧收藏归「其他」。本轮修复朗读收藏来源助手丢失（朗读窗透传 agentId + 后端 sessionPath 兕底）并手帐化语音收藏界面，回归无新增用例（存储/分组层已有覆盖）。Python 新增场景单独通过；全量 runner 仍受既有 Windows Qt 收尾原生退出问题影响，详见 PROJECT_LOG。

## Python 悬浮球

```powershell
python -m py_compile python/zhujian_app.py
```

融合协议相关状态通过 `fusionPanel` 落盘；真实窗口、Ask/朗读继承需重启 Hana 后实机验证。
